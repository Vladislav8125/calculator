# AGENTS.md

## Cursor Cloud specific instructions

This repository ("calculator") is intended for marketing and project economics calculators. As of the initial setup, the repository contains only a `README.md` — no application code, dependencies, or services exist yet.

### Current state

- **No tech stack chosen** — no `package.json`, `requirements.txt`, `Makefile`, or any other dependency/build files.
- **No services to run** — no dev server, database, or background workers.
- **No tests** — no test framework or test files.
- **No lint/format config** — no ESLint, Prettier, Ruff, or similar tools configured.

### When code is added

Once application code is introduced, update this file with:

- How to install dependencies (package manager and commands).
- How to run the dev server and any required services.
- How to run tests and linting.
- Any non-obvious caveats discovered during development.
